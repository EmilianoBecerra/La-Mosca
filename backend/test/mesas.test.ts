/* import test from "node:test";
import assert from "node:assert";
import { crearMesa } from "../utils/mesa.js";
import type { Mesas } from "../interfaces.js";

const jugador = {
  _id: "1",
  nombre: "juan",
  socketID: "223"
}

const mesas: Mesas[] = []



 test("crear-mesas", async () => {
  const nombreMesa = "mesa de pepe";
  const mesa = await crearMesa(jugador, nombreMesa, mesas);
  console.log(mesa.data);
}) */